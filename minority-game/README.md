# 🎯 Minority Wins - 少数派获胜投票游戏

一个基于以太坊/BSC的链上投票游戏，采用"少数派获胜"的独特机制。用户可以创建自定义投票问题，其他用户参与投票，获得最少投票的选项（少数派）将获胜并分享失败者的投注。

## ✨ 特性

- 🗳️ **自定义投票**: 任何用户都可以创建带有自定义问题和选项的投票（2-10个选项）
- 🎲 **少数派获胜**: 获得最少投票的选项获胜，实现博弈论机制
- 🔒 **Commit-Reveal机制**: 防止抢先交易和操纵投票
- 💰 **100%押金模式**: 投票时支付全额押金，简单透明
- 🏆 **奖励分配**: 获胜者按比例分享失败者的投注
- 🎨 **现代化UI**: 美观的渐变色界面，流畅的动画效果
- 🔐 **安全性**: 内置ReentrancyGuard防护，无外部依赖

## 🎮 游戏规则

1. **创建投票**: 用户提出问题并设置2-10个选项
2. **提交阶段** (1小时):
   - 玩家选择选项并提交加密的commit
   - 支付100%押金（即全额投注金额）
3. **揭示阶段** (30分钟):
   - 玩家揭示他们的选择
   - 未揭示的玩家将失去全部押金
4. **结算阶段**:
   - 计算每个选项的总投注
   - 投注最少的选项获胜（少数派）
   - 获胜者按比例分享失败者的投注
5. **领奖阶段**:
   - 获胜者可以领取奖励（本金 + 分配的失败者投注）

## 🚀 快速启动（一键运行）

### Mac/Linux用户

```bash
cd minority-game

# 一键启动所有服务
./start-local.sh

# 在浏览器打开: http://localhost:8000/voting.html
```

这个脚本会自动：
1. ✅ 编译合约
2. ✅ 启动Hardhat本地节点
3. ✅ 部署合约
4. ✅ 配置前端
5. ✅ 启动HTTP服务器

**停止服务：** `./stop-local.sh`

### Windows用户或手动启动

参见下方的 [详细运行步骤](#详细运行步骤)

## 📖 完整文档

- **[前端运行指南](FRONTEND_GUIDE.md)** - 完整的前端配置和使用说明
- **[测试指南](TESTING_GUIDE.md)** - 如何运行测试（18个测试用例）
- **[部署指南](DEPLOYMENT_GUIDE.md)** - BSC测试网/主网部署
- **[编译指南](COMPILE_GUIDE.md)** - 解决编译问题
- **[Stack Too Deep修复](STACK_FIX.md)** - 技术细节

## 📁 项目结构

```
minority-game/
├── contracts/
│   ├── VotingGame.sol          # 主合约
│   └── minority2.sol           # 旧版合约（保留）
├── frontend/
│   ├── voting.html             # 新版前端界面
│   ├── voting.js               # 新版前端逻辑
│   ├── index.html              # 旧版界面（保留）
│   └── app.js                  # 旧版逻辑（保留）
├── scripts/
│   ├── deploy.js               # 部署脚本
│   ├── verify.js               # 验证脚本
│   └── interact.js             # 交互脚本
├── test/
│   └── VotingGame.test.js      # 测试用例
├── hardhat.config.js           # Hardhat配置
├── package.json
└── README.md
```

## 🚀 详细运行步骤

### 前置要求

- Node.js >= 18
- npm 或 yarn
- MetaMask 浏览器扩展
- Python 3（用于启动HTTP服务器）

### 安装依赖

```bash
cd minority-game
npm install
```

### 配置环境变量

复制 `.env.example` 为 `.env` 并填入配置：

```bash
cp .env.example .env
```

编辑 `.env` 文件：

```
PRIVATE_KEY=你的私钥
BSC_TESTNET_URL=https://data-seed-prebsc-1-s1.binance.org:8545
BSCSCAN_API_KEY=你的BSCScan API密钥
```

### 编译合约

```bash
npx hardhat compile
```

### 运行测试

```bash
npx hardhat test
```

测试覆盖：
- ✅ 创建投票功能
- ✅ 提交commit功能
- ✅ 揭示投票功能
- ✅ 结算和奖励计算
- ✅ 领取奖励功能
- ✅ 边界条件测试

### 部署合约

#### 本地测试网

```bash
npx hardhat node
# 在另一个终端
npx hardhat run scripts/deploy.js --network localhost
```

#### BSC 测试网

```bash
npx hardhat run scripts/deploy.js --network bscTestnet
```

#### BSC 主网

```bash
npx hardhat run scripts/deploy.js --network bscMainnet
```

### 验证合约

```bash
npx hardhat run scripts/verify.js --network bscTestnet
```

### 与合约交互

```bash
npx hardhat run scripts/interact.js --network bscTestnet
```

## 🌐 使用前端

### 配置合约地址

1. 部署合约后，将合约地址复制到 `frontend/voting.js`：

```javascript
const CONTRACT_ADDRESS = "你的合约地址";
```

2. 在浏览器中打开 `frontend/voting.html`

### 使用步骤

1. **连接钱包**: 点击"连接 MetaMask"按钮
2. **创建投票**:
   - 输入投票问题
   - 添加选项（2-10个）
   - 点击"创建投票"
3. **参与投票**:
   - 切换到"参与投票"标签
   - 选择一个活跃的投票
   - 选择你的答案并输入投注金额
   - 提交commit
4. **揭示投票**:
   - 在揭示阶段，提交reveal
5. **领取奖励**:
   - 在"我的投票"标签查看结果
   - 如果获胜，点击"领取奖励"

## 🧪 测试用例说明

### 创建投票测试
- 验证正常创建投票
- 验证选项数量限制（2-10个）
- 验证问题和选项不能为空

### Commit阶段测试
- 验证正常提交commit
- 验证必须支付押金
- 验证不能重复commit
- 验证时间限制

### Reveal阶段测试
- 验证正常揭示投票
- 验证哈希匹配
- 验证押金金额范围（30-70%）
- 验证时间限制

### 结算测试
- 验证少数派正确识别
- 验证奖励计算准确
- 验证获胜者可以领取奖励
- 验证失败者没有奖励
- 验证未揭示者失去押金

### 查询功能测试
- 验证获取活跃投票
- 验证获取参与者列表

## 📊 智能合约 API

### 主要函数

#### createVote
创建新投票
```solidity
function createVote(string memory question, string[] memory options)
    external returns (uint256)
```

#### commit
提交加密的投票commit
```solidity
function commit(uint256 voteId, bytes32 commitHash)
    external payable
```

#### reveal
揭示投票选择
```solidity
function reveal(uint256 voteId, uint256 choice, bytes32 secret)
    external
```

#### finalizeVote
结算投票
```solidity
function finalizeVote(uint256 voteId)
    external
```

#### claimReward
领取奖励
```solidity
function claimReward(uint256 voteId)
    external
```

### 查询函数

#### getVoteInfo
获取投票信息
```solidity
function getVoteInfo(uint256 voteId)
    external view returns (...)
```

#### calculateReward
计算玩家奖励
```solidity
function calculateReward(uint256 voteId, address player)
    external view returns (uint256)
```

#### getAllActiveVotes
获取所有活跃投票
```solidity
function getAllActiveVotes()
    external view returns (uint256[] memory)
```

## 🔐 安全特性

1. **ReentrancyGuard**: 防止重入攻击
2. **Commit-Reveal**: 防止抢先交易
3. **押金机制**: 防止恶意提交
4. **时间锁**: 确保游戏公平性
5. **输入验证**: 严格的参数检查

## 🎨 前端特性

- 📱 响应式设计，支持移动端
- 🎭 流畅的动画效果
- 🎨 现代化渐变色UI
- 💾 本地存储commit信息
- 🔄 实时状态更新
- ⚡ 快速的交互体验

## 📝 开发笔记

### Commit Hash 计算（100%押金模式）

```javascript
const commitHash = ethers.keccak256(
    ethers.solidityPacked(
        ['uint256', 'uint256', 'bytes32', 'address'],
        [voteId, choice, secret, userAddress]
    )
);
```

**注意**: betAmount不再包含在commitHash中，因为采用100%押金模式。

### 押金计算

100%押金模式：投注时支付全额
```javascript
// commit时直接支付全额
await contract.commit(voteId, commitHash, { value: betAmount });
```

### 奖励计算（100%押金模式）

获胜者奖励 = 投注 + (失败者总投注 × 个人投注 / 获胜者总投注)

```solidity
uint256 share = (losingTotal * playerCommit.betAmount) / winningTotal;
return playerCommit.betAmount + share;
```

## 🐛 常见问题

### Q: 为什么需要100%押金？
A: 100%押金模式简化了游戏机制，让规则更加透明。用户提交commit时直接支付全部投注金额，防止commit后不揭示的行为。

### Q: 如果所有选项得票相同怎么办？
A: 系统将识别为平局，所有玩家取回投注。

### Q: 揭示阶段错过了怎么办？
A: 错过揭示阶段将失去全部押金（即投注金额），这是为了惩罚不诚实的行为。

### Q: 可以创建多少个选项？
A: 最少2个，最多10个选项。

### Q: 如何打开前端网页？
A:
- **Mac/Linux**: 运行 `./start-local.sh` 后访问 http://localhost:8000/voting.html
- **Windows**: 参见 [FRONTEND_GUIDE.md](FRONTEND_GUIDE.md) 的详细说明
- **需要**: MetaMask浏览器插件已安装并配置

## 📜 许可证

MIT License

## 👥 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 联系方式

如有问题或建议，请通过以下方式联系：
- 提交 GitHub Issue
- 发送邮件至项目维护者

---

**注意**: 这是一个实验性项目，请在测试网上充分测试后再部署到主网。投资有风险，参与需谨慎！
