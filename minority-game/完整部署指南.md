# 🚀 完整部署指南 - 解决MetaMask警告

## 问题诊断

你遇到的两个问题：

1. ❌ **MetaMask警告"会拿走所有资产"**
   - 原因：前端还在使用默认地址 `0x5FbDB2315678afecb367f032d93F642f64180aa3`
   - 这个地址被MetaMask标记为恶意地址

2. ❌ **加载Active Votes失败**
   - 原因：Hardhat节点没有运行，或者合约没有正确部署

## ✅ 完整解决方案（5分钟搞定）

### 步骤1️⃣：启动Hardhat节点（终端1）

```bash
cd /home/user/dapp/minority-game
npx hardhat node
```

**重要：**
- 保持这个终端运行，不要关闭！
- 你会看到10个测试账户和它们的私钥

### 步骤2️⃣：部署合约到新地址（终端2）

等步骤1完成后，打开新终端：

```bash
cd /home/user/dapp/minority-game
npx hardhat run scripts/deploy-alt-address.js --network localhost
```

**你应该看到：**
```
🚀 使用备用账户部署 VotingGame 合约（避开MetaMask警告）...

✅ 部署账户（账户#1）: 0x70997970C51812dc3A010C7d01b50e0d17dc79C8

🎉 VotingGame 合约已部署!
📍 合约地址: 0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512
⚠️  这个地址与默认地址不同，MetaMask不会警告!

✅ frontend/voting.js 已自动更新合约地址!
```

**检查点：**
- ✅ 新地址不是 `0x5FbDB...`
- ✅ 看到"frontend/voting.js 已自动更新"

### 步骤3️⃣：启动前端服务器（终端3）

```bash
cd /home/user/dapp/minority-game/frontend
python3 -m http.server 8000
```

### 步骤4️⃣：配置MetaMask

#### A. 添加Localhost网络（首次需要）

1. 打开MetaMask
2. 点击网络下拉 → "添加网络" → "手动添加"
3. 填写：
   - **网络名称**: Localhost 8545
   - **RPC URL**: `http://127.0.0.1:8545`
   - **Chain ID**: `1337`
   - **货币符号**: ETH
4. 保存

#### B. 导入测试账户（推荐）

从步骤1的终端输出中，找到Account #1：

```
Account #1: 0x70997970C51812dc3A010C7d01b50e0d17dc79C8 (10000 ETH)
Private Key: 0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d
```

在MetaMask中：
1. 点击账户图标 → "导入账户"
2. 粘贴私钥：`0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d`
3. 导入

#### C. 切换网络

在MetaMask中选择 "Localhost 8545"

### 步骤5️⃣：测试

1. 浏览器访问：`http://localhost:8000/voting.html`
2. **刷新页面**（Ctrl+Shift+R 强制刷新）
3. 点击 "Connect Wallet"
4. 尝试创建投票

**预期结果：**
- ✅ MetaMask弹出交易确认窗口
- ✅ **没有红色警告**
- ✅ **没有"会拿走所有资产"的提示**
- ✅ Active Votes可以正常加载

---

## 🔍 验证是否成功

### 在浏览器控制台（F12）检查

```javascript
console.log(CONTRACT_ADDRESS);
```

**应该输出：** `0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512`（或类似的新地址）

**不应该是：** `0x5FbDB2315678afecb367f032d93F642f64180aa3`

---

## 🛠️ 故障排查

### 问题：前端还显示旧地址

**检查：**
```bash
grep CONTRACT_ADDRESS frontend/voting.js | head -1
```

**应该显示新地址，如果还是旧地址，手动更新：**
```bash
# 手动更新前端配置
cd /home/user/dapp/minority-game
NEW_ADDR=$(cat deployment-info.json | grep contractAddress | cut -d'"' -f4)
sed -i "s/const CONTRACT_ADDRESS = \"0x[a-fA-F0-9]*\"/const CONTRACT_ADDRESS = \"$NEW_ADDR\"/" frontend/voting.js
echo "✅ 已更新为: $NEW_ADDR"
```

### 问题：MetaMask还是警告

**可能原因：**
1. 浏览器缓存 - 强制刷新页面（Ctrl+Shift+R）
2. 还在使用旧地址 - 检查上面的验证步骤
3. MetaMask缓存 - Settings → Advanced → Clear activity tab data

### 问题："Nonce too high"

**解决：**
1. MetaMask → Settings → Advanced
2. Clear activity tab data
3. 或重启Hardhat节点

### 问题：Active Votes加载失败

**检查：**
1. Hardhat节点是否在运行（终端1）
2. MetaMask是否连接到Localhost 8545
3. 合约是否部署成功（检查终端2的输出）

---

## 📊 地址对比

| 项目 | 旧地址（被封） | 新地址（安全） |
|------|---------------|---------------|
| 地址 | `0x5FbDB2315678afecb367f032d93F642f64180aa3` | `0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512` |
| 部署账户 | Account #0 | Account #1 |
| MetaMask | ❌ 警告 | ✅ 通过 |

---

## 🎉 成功标志

如果一切正常，你应该看到：

1. **创建投票**
   - 点击 "CREATE VOTE"
   - MetaMask弹出，显示交易详情
   - **无红色警告**
   - **无"欺诈请求"提示**
   - 确认后，看到 "✅ 投票创建成功!"

2. **Active Votes列表**
   - 自动刷新显示新投票
   - 无错误提示

3. **浏览器控制台**
   - 无红色错误
   - 合约地址是新地址

---

## ⚠️ 重要提醒

### 仅用于本地开发

这些Hardhat测试账户的私钥是公开的：
- ✅ 可以在本地使用
- ❌ **绝对不要**在真实网络使用
- ❌ **绝对不要**向这些地址发送真实ETH

### 每次重启Hardhat节点

- 所有数据会重置
- 需要重新部署合约
- 可能需要清除MetaMask的活动数据

### 正式部署

如果要部署到BSC测试网或主网：
- 使用你自己的私钥
- 参考 `DEPLOY_WITH_REMIX.md`
- 或使用 `scripts/deploy.js` 配合 `.env` 文件

---

## 🎯 快速命令参考

### 完整重启流程

**停止所有：**
```bash
# 停止Hardhat节点
pkill -f "hardhat node"

# 停止HTTP服务器
pkill -f "http.server"
```

**重新启动：**
```bash
# 终端1
cd /home/user/dapp/minority-game && npx hardhat node

# 终端2（等终端1启动完）
cd /home/user/dapp/minority-game && npx hardhat run scripts/deploy-alt-address.js --network localhost

# 终端3
cd /home/user/dapp/minority-game/frontend && python3 -m http.server 8000
```

---

## 📞 还有问题？

如果按照以上步骤还有问题：

1. **检查终端输出**的错误信息
2. **检查浏览器控制台**（F12）的错误
3. **检查MetaMask**的网络设置
4. **尝试完整重启**（上面的快速命令）

祝开发顺利！🚀
